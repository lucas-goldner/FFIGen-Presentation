import 'package:flutter/widgets.dart';

class SlideTitleAndPhoto extends StatelessWidget {
  const SlideTitleAndPhoto({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
